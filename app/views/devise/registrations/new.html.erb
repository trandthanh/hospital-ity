<% if params[:super_host] %>
  <h2>Devenir hôte-bénévole</h2>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>
    <% @user.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
    <div class="form-inputs">

      <%= f. hidden_field :super_host, :value => true %>
      <%#= f.input :code_id, :label_method => :code %>

      <%= f.input :family,
                  required: true,
                  input_html: { autocomplete: "family" }%>
      <%= f.input :first_name,
                  required: true,
                  input_html: { autocomplete: "first_name" }%>
      <%= f.input :hospital_id, collection: Hospital.all, :label_method => :hospital_name,
                  required: true,
                  input_html: { autocomplete: "hospital_id" }%>
      <%= f.input :email,
                  required: true,
                  autofocus: true ,
                  input_html: { autocomplete: "email" }%>
      <%= f.input :password,
                  required: true,
                  hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  required: true,
                  input_html: { autocomplete: "new-password" } %>
    </div>
    <div class="form-actions">
      <%= f.button :submit, "Sign up" %>
    </div>
  <% end %>
<% elsif params[:hospital_admin] %>

  <h2>Admin d'un hôpital ?</h2>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>
    <% @user.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
    <div class="form-inputs">

      <%= f. hidden_field :hospital_admin, :value => true %>
      <%#= f.input :code_id, :label_method => :code %>

      <%= f.input :family,
                  required: true,
                  input_html: { autocomplete: "family" }%>
      <%= f.input :first_name,
                  required: true,
                  input_html: { autocomplete: "first_name" }%>
      <%= f.input :hospital_id, collection: Hospital.all, :label_method => :hospital_name,
                  required: true,
                  input_html: { autocomplete: "hospital_id" }%>
      <%= f.input :email,
                  required: true,
                  autofocus: true ,
                  input_html: { autocomplete: "email" }%>
      <%= f.input :password,
                  required: true,
                  hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  required: true,
                  input_html: { autocomplete: "new-password" } %>
    </div>
    <div class="form-actions">
      <%= f.button :submit, "Sign up" %>
    </div>
  <% end %>



<% else %>
  <h2>Bénéficier de Hospital.ité</h2>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>
    <% @user.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
    <div class="form-inputs">
      <%= f.input :family,
                  required: true,
                  input_html: { autocomplete: "family" }%>
      <%= f.input :first_name,
                  required: true,
                  input_html: { autocomplete: "first_name" }%>
      <%= f.input :family_member,
                  required: true,
                  input_html: { autocomplete: "family_member" }%>
      <%= f.input :hospital_id, collection: Hospital.all, :label_method => :hospital_name,
                  required: true,
                  input_html: { autocomplete: "hospital_id" }%>
      <%= f.input :email,
                  required: true,
                  input_html: { autocomplete: "email" }%>
      <%#= f.input :code_id, :label_method => :code %>
      <%= simple_fields_for :other do |o| %>
        <%= o.input :code, label: 'Renseignez le code donné' %>
      <% end %>
      <%= f.input :password,
                  required: true,
                  hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  required: true,
                  input_html: { autocomplete: "new-password" } %>
    </div>
    <div class="form-actions">
      <%= f.button :submit, "Sign up" %>
    </div>
  <% end %>
<% end %>

<%= render "devise/shared/links" %>
